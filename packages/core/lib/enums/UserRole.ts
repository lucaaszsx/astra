/**
 * @file UserRole.ts
 * @description Enum representing the possible roles a user can have within the system.
 * @author Lucas
 * @license MIT
 */

export enum UserRole {
    ADMIN = 'admin',
    USER  = 'user'
}